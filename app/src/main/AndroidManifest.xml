<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713"/>

        <!-- Main Activity (Launcher) -->
        <service
            android:name=".PomodoroService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_TYPE"
                android:value="Pomodoro timer needs to keep running in background to notify user."/>
        </service>

        <receiver android:name=".PomodoroReceiver" android:exported="false"/>
        <receiver android:name=".TimerActionReceiver" android:exported="false"/>
        <receiver android:name=".Tasks.MyNotificationPublisher" android:exported="false"/>

        <receiver android:name=".PomodoroWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/pomodoro_widget_info" />
        </receiver>

        <activity
            android:name="com.denzo.mypomodoro.MainActivity"
            android:label="@string/app_name"
            android:exported="false">
        </activity>

        <activity
            android:name="com.denzo.mypomodoro.OnboardingActivity"
            android:label="Tutorial"
            android:exported="false"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name="com.denzo.mypomodoro.SplashActivity"
            android:label="@string/app_name"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- About Page -->
        <activity
            android:name="com.denzo.mypomodoro.AboutActivity"
            android:exported="false">
        </activity>

        <!-- Signup Activity -->
        <activity
            android:name="com.denzo.mypomodoro.SignUpActivity"
            android:label="Sign Up"
            android:exported="false">
        </activity>

        <!-- Signin Activity -->
        <activity
            android:name="com.denzo.mypomodoro.SignInActivity"
            android:label="Sign In"
            android:exported="false">
        </activity>

        <!-- App Lock -->
        <activity
            android:name="com.denzo.mypomodoro.applicationlock.ApplicationLockActivity"
            android:label="App Lock"
            android:exported="false">
        </activity>

        <!-- Services -->
        <service android:name="com.denzo.mypomodoro.NotificationService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_TYPE"
                android:value="Timer progress updates in notification."/>
        </service>

        <service android:name="com.denzo.mypomodoro.EndNotificationService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_TYPE"
                android:value="Notify user when pomodoro session ends."/>
        </service>

        <service
            android:name="com.denzo.mypomodoro.applicationlock.ApplicationLockService"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.view.accessibility.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>

</manifest>
